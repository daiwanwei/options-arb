[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
config = "0.15"
dotenvy = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["blocking", "json", "rustls-tls"] }
thiserror = "2"
